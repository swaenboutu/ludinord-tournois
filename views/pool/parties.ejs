<%- include('../partials/header', { title, wide: true }) %>

<%- include('../partials/breadcrumb', { items: [
  { label: 'Tournois', href: '/tournaments' },
  { label: tournament.name, href: `/tournaments/${tournament.id}` },
  { label: 'Phase de poule', href: `/tournaments/${tournament.id}/pool` },
  { label: `Manche ${position} — ${round.game_name}` },
] }) %>

<%- include('../partials/parties-tables', {
  basePath: `/tournaments/${tournament.id}/pool`,
  round,
  position,
  parties,
  error,
  emptyHint: "Aucune table tirée. Vérifie qu'il y a des équipes dans le tournoi, puis lance le tirage.",
}) %>

<%- include('../partials/footer') %>
