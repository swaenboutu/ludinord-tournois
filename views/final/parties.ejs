<%- include('../partials/header', { title, wide: true }) %>

<%- include('../partials/breadcrumb', { items: [
  { label: 'Tournois', href: '/tournaments' },
  { label: tournament.name, href: `/tournaments/${tournament.id}` },
  { label: 'Phase finale', href: `/tournaments/${tournament.id}/final` },
  { label: stage.name, href: `/tournaments/${tournament.id}/final/stages/${stage.id}` },
  { label: `Manche ${position} — ${round.game_name}` },
] }) %>

<%- include('../partials/parties-tables', {
  basePath: `/tournaments/${tournament.id}/final`,
  round,
  position,
  parties,
  error,
  emptyHint: "Aucune table tirée. Vérifie que les équipes de l'étape sont qualifiées, puis lance le tirage.",
}) %>

<%- include('../partials/footer') %>
