<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <main class="container <%= (typeof wide !== 'undefined' && wide) ? 'container-wide' : '' %>">
    <div class="admin-topbar">
      <a href="/tournaments" class="admin-logo-link" title="Retour à la liste des tournois">
        <img src="/images/ludinord-logo.svg" alt="LudiNord — Tournois" class="admin-logo">
      </a>
      <form method="post" action="/logout">
        <button type="submit" class="link-button">Déconnexion</button>
      </form>
    </div>
